Codebase is currently centered in src/main.rs with adapter modules under src/adapters. Conventions observed: snake_case for functions/files, CamelCase for types/enums, small helper functions for parsing and command execution, Result-based error propagation with user-facing String messages in UI pathways. Serde derives on config/data structs; enums use serde rename_all when persisted. Keep behavior/messages stable unless requested.