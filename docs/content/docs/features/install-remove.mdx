---
title: Install and Remove
description: Safe skill lifecycle operations through the skills CLI.
---

Install and remove actions are executed via the configured `skills` command.

- install from selected result,
- remove with confirmation flows,
- keep lock/config state consistent after changes.

Because lazyskills delegates to the CLI, behavior matches your existing tooling expectations.
